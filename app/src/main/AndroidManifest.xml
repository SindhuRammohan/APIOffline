<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.real_estate.apioffline">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <application
        android:name=".ContactListApplication"
        android:allowBackup="true"
        android:icon="@mipmap/balanjika"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/balanjika"
        android:supportsRtl="true"
        android:theme="@style/Theme.APIOffline">
        <activity android:name=".features.contactlist.WebViewActivity" />
        <activity android:name="com.real_estate.pdfkotlin.PdfViewActivity" />
        <activity
            android:name=".features.contactlist.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>